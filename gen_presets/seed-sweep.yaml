# Seed sweep generation preset — same prompt, different seeds
# Usage: drawagent run --gen-params gen_presets/seed-sweep.yaml "your prompt"

width: 1024
height: 1024
steps: 8
guidance: 3.5
seed: -1
num_images: 4
